export interface RoleConfigs {
  name: string;
  checked: boolean;
  disabled: boolean;
  exact: boolean;
  expanded: boolean;
  includeHidden: boolean;
  level: number;
  pressed: boolean;
  selected: boolean;
}
